apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ddaa
  labels:
    app: frontend
spec:
  # Único punto público de entrada a la aplicación.
  type: LoadBalancer
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80