apiVersion: v1
kind: Service
metadata:
  name: sqlserver
  namespace: ddaa
  labels:
    app: sqlserver
spec:
  # La base de datos queda disponible solo dentro del clúster.
  type: ClusterIP
  selector:
    app: sqlserver
  ports:
    - name: sqlserver
      port: 1433
      targetPort: 1433