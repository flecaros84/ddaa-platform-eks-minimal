apiVersion: v1
kind: Service
metadata:
  name: ddaa-service
  namespace: ddaa
  labels:
    app: ddaa-service
spec:
  # El backend queda disponible solo dentro del clúster.
  type: ClusterIP
  selector:
    app: ddaa-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082